{
  "name": "hexlet-code",
  "private": true,
  "scripts": {
    "build": "npm run build:css",
    "build:css": "npx --no-install @tailwindcss/cli -i ./assets/app.css -o ./page_analyzer/static/style.css --minify",
    "watch:css": "npx --no-install @tailwindcss/cli -i ./assets/app.css -o ./page_analyzer/static/style.css --watch"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^4.1.12"
  }
}
